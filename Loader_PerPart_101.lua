
do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part001.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 1")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 1:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 1:", err)
        end
    else
        warn("❌ Gagal ambil Part 1")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part002.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 2")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 2:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 2:", err)
        end
    else
        warn("❌ Gagal ambil Part 2")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part003.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 3")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 3:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 3:", err)
        end
    else
        warn("❌ Gagal ambil Part 3")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part004.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 4")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 4:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 4:", err)
        end
    else
        warn("❌ Gagal ambil Part 4")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part005.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 5")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 5:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 5:", err)
        end
    else
        warn("❌ Gagal ambil Part 5")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part006.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 6")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 6:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 6:", err)
        end
    else
        warn("❌ Gagal ambil Part 6")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part007.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 7")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 7:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 7:", err)
        end
    else
        warn("❌ Gagal ambil Part 7")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part008.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 8")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 8:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 8:", err)
        end
    else
        warn("❌ Gagal ambil Part 8")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part009.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 9")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 9:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 9:", err)
        end
    else
        warn("❌ Gagal ambil Part 9")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part010.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 10")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 10:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 10:", err)
        end
    else
        warn("❌ Gagal ambil Part 10")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part011.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 11")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 11:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 11:", err)
        end
    else
        warn("❌ Gagal ambil Part 11")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part012.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 12")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 12:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 12:", err)
        end
    else
        warn("❌ Gagal ambil Part 12")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part013.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 13")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 13:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 13:", err)
        end
    else
        warn("❌ Gagal ambil Part 13")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part014.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 14")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 14:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 14:", err)
        end
    else
        warn("❌ Gagal ambil Part 14")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part015.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 15")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 15:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 15:", err)
        end
    else
        warn("❌ Gagal ambil Part 15")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part016.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 16")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 16:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 16:", err)
        end
    else
        warn("❌ Gagal ambil Part 16")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part017.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 17")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 17:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 17:", err)
        end
    else
        warn("❌ Gagal ambil Part 17")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part018.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 18")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 18:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 18:", err)
        end
    else
        warn("❌ Gagal ambil Part 18")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part019.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 19")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 19:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 19:", err)
        end
    else
        warn("❌ Gagal ambil Part 19")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part020.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 20")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 20:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 20:", err)
        end
    else
        warn("❌ Gagal ambil Part 20")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part021.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 21")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 21:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 21:", err)
        end
    else
        warn("❌ Gagal ambil Part 21")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part022.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 22")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 22:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 22:", err)
        end
    else
        warn("❌ Gagal ambil Part 22")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part023.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 23")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 23:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 23:", err)
        end
    else
        warn("❌ Gagal ambil Part 23")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part024.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 24")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 24:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 24:", err)
        end
    else
        warn("❌ Gagal ambil Part 24")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part025.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 25")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 25:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 25:", err)
        end
    else
        warn("❌ Gagal ambil Part 25")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part026.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 26")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 26:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 26:", err)
        end
    else
        warn("❌ Gagal ambil Part 26")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part027.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 27")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 27:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 27:", err)
        end
    else
        warn("❌ Gagal ambil Part 27")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part028.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 28")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 28:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 28:", err)
        end
    else
        warn("❌ Gagal ambil Part 28")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part029.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 29")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 29:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 29:", err)
        end
    else
        warn("❌ Gagal ambil Part 29")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part030.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 30")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 30:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 30:", err)
        end
    else
        warn("❌ Gagal ambil Part 30")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part031.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 31")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 31:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 31:", err)
        end
    else
        warn("❌ Gagal ambil Part 31")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part032.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 32")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 32:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 32:", err)
        end
    else
        warn("❌ Gagal ambil Part 32")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part033.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 33")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 33:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 33:", err)
        end
    else
        warn("❌ Gagal ambil Part 33")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part034.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 34")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 34:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 34:", err)
        end
    else
        warn("❌ Gagal ambil Part 34")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part035.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 35")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 35:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 35:", err)
        end
    else
        warn("❌ Gagal ambil Part 35")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part036.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 36")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 36:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 36:", err)
        end
    else
        warn("❌ Gagal ambil Part 36")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part037.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 37")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 37:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 37:", err)
        end
    else
        warn("❌ Gagal ambil Part 37")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part038.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 38")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 38:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 38:", err)
        end
    else
        warn("❌ Gagal ambil Part 38")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part039.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 39")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 39:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 39:", err)
        end
    else
        warn("❌ Gagal ambil Part 39")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part040.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 40")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 40:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 40:", err)
        end
    else
        warn("❌ Gagal ambil Part 40")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part041.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 41")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 41:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 41:", err)
        end
    else
        warn("❌ Gagal ambil Part 41")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part042.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 42")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 42:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 42:", err)
        end
    else
        warn("❌ Gagal ambil Part 42")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part043.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 43")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 43:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 43:", err)
        end
    else
        warn("❌ Gagal ambil Part 43")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part044.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 44")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 44:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 44:", err)
        end
    else
        warn("❌ Gagal ambil Part 44")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part045.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 45")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 45:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 45:", err)
        end
    else
        warn("❌ Gagal ambil Part 45")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part046.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 46")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 46:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 46:", err)
        end
    else
        warn("❌ Gagal ambil Part 46")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part047.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 47")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 47:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 47:", err)
        end
    else
        warn("❌ Gagal ambil Part 47")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part048.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 48")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 48:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 48:", err)
        end
    else
        warn("❌ Gagal ambil Part 48")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part049.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 49")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 49:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 49:", err)
        end
    else
        warn("❌ Gagal ambil Part 49")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part050.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 50")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 50:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 50:", err)
        end
    else
        warn("❌ Gagal ambil Part 50")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part051.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 51")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 51:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 51:", err)
        end
    else
        warn("❌ Gagal ambil Part 51")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part052.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 52")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 52:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 52:", err)
        end
    else
        warn("❌ Gagal ambil Part 52")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part053.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 53")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 53:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 53:", err)
        end
    else
        warn("❌ Gagal ambil Part 53")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part054.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 54")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 54:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 54:", err)
        end
    else
        warn("❌ Gagal ambil Part 54")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part055.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 55")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 55:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 55:", err)
        end
    else
        warn("❌ Gagal ambil Part 55")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part056.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 56")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 56:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 56:", err)
        end
    else
        warn("❌ Gagal ambil Part 56")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part057.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 57")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 57:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 57:", err)
        end
    else
        warn("❌ Gagal ambil Part 57")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part058.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 58")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 58:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 58:", err)
        end
    else
        warn("❌ Gagal ambil Part 58")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part059.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 59")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 59:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 59:", err)
        end
    else
        warn("❌ Gagal ambil Part 59")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part060.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 60")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 60:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 60:", err)
        end
    else
        warn("❌ Gagal ambil Part 60")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part061.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 61")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 61:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 61:", err)
        end
    else
        warn("❌ Gagal ambil Part 61")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part062.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 62")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 62:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 62:", err)
        end
    else
        warn("❌ Gagal ambil Part 62")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part063.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 63")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 63:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 63:", err)
        end
    else
        warn("❌ Gagal ambil Part 63")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part064.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 64")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 64:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 64:", err)
        end
    else
        warn("❌ Gagal ambil Part 64")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part065.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 65")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 65:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 65:", err)
        end
    else
        warn("❌ Gagal ambil Part 65")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part066.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 66")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 66:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 66:", err)
        end
    else
        warn("❌ Gagal ambil Part 66")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part067.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 67")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 67:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 67:", err)
        end
    else
        warn("❌ Gagal ambil Part 67")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part068.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 68")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 68:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 68:", err)
        end
    else
        warn("❌ Gagal ambil Part 68")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part069.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 69")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 69:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 69:", err)
        end
    else
        warn("❌ Gagal ambil Part 69")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part070.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 70")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 70:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 70:", err)
        end
    else
        warn("❌ Gagal ambil Part 70")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part071.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 71")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 71:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 71:", err)
        end
    else
        warn("❌ Gagal ambil Part 71")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part072.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 72")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 72:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 72:", err)
        end
    else
        warn("❌ Gagal ambil Part 72")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part073.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 73")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 73:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 73:", err)
        end
    else
        warn("❌ Gagal ambil Part 73")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part074.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 74")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 74:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 74:", err)
        end
    else
        warn("❌ Gagal ambil Part 74")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part075.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 75")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 75:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 75:", err)
        end
    else
        warn("❌ Gagal ambil Part 75")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part076.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 76")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 76:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 76:", err)
        end
    else
        warn("❌ Gagal ambil Part 76")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part077.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 77")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 77:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 77:", err)
        end
    else
        warn("❌ Gagal ambil Part 77")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part078.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 78")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 78:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 78:", err)
        end
    else
        warn("❌ Gagal ambil Part 78")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part079.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 79")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 79:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 79:", err)
        end
    else
        warn("❌ Gagal ambil Part 79")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part080.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 80")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 80:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 80:", err)
        end
    else
        warn("❌ Gagal ambil Part 80")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part081.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 81")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 81:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 81:", err)
        end
    else
        warn("❌ Gagal ambil Part 81")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part082.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 82")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 82:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 82:", err)
        end
    else
        warn("❌ Gagal ambil Part 82")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part083.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 83")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 83:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 83:", err)
        end
    else
        warn("❌ Gagal ambil Part 83")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part084.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 84")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 84:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 84:", err)
        end
    else
        warn("❌ Gagal ambil Part 84")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part085.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 85")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 85:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 85:", err)
        end
    else
        warn("❌ Gagal ambil Part 85")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part086.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 86")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 86:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 86:", err)
        end
    else
        warn("❌ Gagal ambil Part 86")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part087.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 87")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 87:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 87:", err)
        end
    else
        warn("❌ Gagal ambil Part 87")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part088.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 88")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 88:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 88:", err)
        end
    else
        warn("❌ Gagal ambil Part 88")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part089.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 89")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 89:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 89:", err)
        end
    else
        warn("❌ Gagal ambil Part 89")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part090.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 90")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 90:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 90:", err)
        end
    else
        warn("❌ Gagal ambil Part 90")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part091.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 91")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 91:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 91:", err)
        end
    else
        warn("❌ Gagal ambil Part 91")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part092.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 92")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 92:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 92:", err)
        end
    else
        warn("❌ Gagal ambil Part 92")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part093.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 93")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 93:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 93:", err)
        end
    else
        warn("❌ Gagal ambil Part 93")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part094.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 94")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 94:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 94:", err)
        end
    else
        warn("❌ Gagal ambil Part 94")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part095.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 95")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 95:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 95:", err)
        end
    else
        warn("❌ Gagal ambil Part 95")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part096.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 96")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 96:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 96:", err)
        end
    else
        warn("❌ Gagal ambil Part 96")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part097.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 97")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 97:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 97:", err)
        end
    else
        warn("❌ Gagal ambil Part 97")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part098.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 98")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 98:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 98:", err)
        end
    else
        warn("❌ Gagal ambil Part 98")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part099.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 99")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 99:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 99:", err)
        end
    else
        warn("❌ Gagal ambil Part 99")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part100.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 100")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 100:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 100:", err)
        end
    else
        warn("❌ Gagal ambil Part 100")
    end

    task.wait(0.05)
end

do
    local url = "https://raw.githubusercontent.com/GragsLifer/Yes/main/Secret_Part101.lua"
    local success, result = pcall(function()
        return game:HttpGet(url)
    end)

    if success and result and result ~= "" then
        print("✅ Loaded Part 101")
        local fn, err = loadstring(result)
        if fn then
            local ok, execErr = pcall(fn)
            if not ok then
                warn("⚠️ Error saat menjalankan Part 101:", execErr)
            end
        else
            warn("❌ Gagal loadstring di Part 101:", err)
        end
    else
        warn("❌ Gagal ambil Part 101")
    end

    task.wait(0.05)
end
